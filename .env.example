# Thetaglass — environment template. Copy to `.env` (gitignored) and fill in.
#
# NOTE: Robinhood is authenticated via the MCP OAuth flow (phone-approved once),
# NOT via keys in this file. Nothing here grants trading access, and Thetaglass
# is read-only — it never places, modifies, or closes orders.

# --- Notifications (optional; Thetaglass's push channel) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
